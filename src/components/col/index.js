export default {
  props: {
    spans: Number,
    offset: Number,
    push: Number,
    pull: Number
  },

  methods: {
    getStyle() {
      // let style = this.$parent.getStyle()
    }
  },

  render(h) {
    return <div />
  }
}
